# Cloudflare Pages automatically handles SPA routing
# If 404.html doesn't exist, Pages serves root / for unmatched paths
# Static files (JS, CSS, etc.) are served automatically
# No redirect rules needed - Cloudflare Pages handles this automatically
