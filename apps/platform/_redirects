# Cloudflare Pages SPA Redirect
# Static assets are served first, then SPA fallback for HTML routes only

/remoteEntry.js   /remoteEntry.js   200
/*.js             /:splat           200
/*.css            /:splat           200
/*.json           /:splat           200
/*.map            /:splat           200
/*                /index.html       200
